#!/usr/bin/env ruby

require_relative '../lib/move.rb'

greeting
board = 'board'
board = Array.new
board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]
question
input = gets.strip
index = input_to_index(input)
move(board, index = "X")
display_board(board)
